{
  "buildCommand": "pnpm --filter shared build && pnpm --filter frontend build && node_modules/.bin/esbuild api/source.ts --bundle --platform=node --format=cjs --outfile=api/index.js --external:pg-native",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/health", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
